algoritmo "Passo_a_passo_7"

var
num, cont, soma: inteiro
result: real
inicio
cont <- 0
result <- 0

enqaunto num >= 0 faca
escreval("Informe um número")
leia(num)
se num >= 0 entao
cont <- cont+1
soma <- soma + num
fimse
fimalgoritmo

